/**
 * NPBN Cookie Consent — Admin color picker initialization.
 *
 * @package NPBN_Cookie_Consent
 */
(function ($) {
	$(document).ready(function () {
		$('.npbn-color-picker').wpColorPicker();
	});
})(jQuery);
